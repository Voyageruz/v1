import 'package:flutter/material.dart';

class PackesScreen extends StatelessWidget {
  const PackesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Trip Packs\nSoon',
        textAlign: TextAlign.center,
      ),
    );
  }
}
